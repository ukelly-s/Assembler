#include "asm.h"

void	*parse(t_parser *assm)
{
	char		*line;

//	while (get_next_line(assm->fd, &line) > 0)
//	{
//		if ()
//			;
//		else if ()
//			;
//		else
//			ft_kill("Error", NULL, __func__, __FILE__);
//	}

}